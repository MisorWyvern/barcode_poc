import 'package:flutter/material.dart';

class MLScanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("MLSCANPAGE"),
    );
  }
}
